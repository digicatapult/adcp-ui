import React from 'react'
import styled from '@emotion/styled'

const Orchestrator = () => {
  return <Wrapper>Orchestrator Main</Wrapper>
}

const Wrapper = styled.div`
  grid-area: main;
`

export default Orchestrator
