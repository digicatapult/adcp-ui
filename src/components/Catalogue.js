import React from 'react'
import styled from 'styled-components'

const Catalogue = () => {
  return <Wrapper>Catalogue Main</Wrapper>
}

const Wrapper = styled.div`
  grid-area: main;
`

export default Catalogue
