import React from 'react'
import styled from 'styled-components'

const Profiler = () => {
  return <Wrapper>Profiler Main</Wrapper>
}

const Wrapper = styled.div`
  grid-area: main;
`

export default Profiler
